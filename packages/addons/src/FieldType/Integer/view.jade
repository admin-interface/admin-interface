.form-group
    small= $field.getTitle()
    .form-line(class= $field.isDisabled() ? 'disabled' : '')
        input(
        type="number",
        name= $field.getField(),
        value= value,
        disabled= $field.isDisabled()
        ).form-control